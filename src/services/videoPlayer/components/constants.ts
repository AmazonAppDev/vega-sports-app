/** The time in ms to throttle seeking interactions by */
export const SEEKING_INTERACTIONS_THROTTLE_MS = 200;

/** The smallest amount of time in ms to seek by */
export const SEEKING_STEP_MS = 10;

/** The time in ms to throttle time-related event updates by so as not to flood the video player (which crashes the app) */
export const TIME_RELATED_UPDATES_THROTTLE_MS = 500;
